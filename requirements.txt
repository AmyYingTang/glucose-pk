# Flask 核心
flask>=2.0.0
python-dotenv>=1.0.0
gunicorn>=21.0.0

# Dexcom API
pydexcom>=0.2.0

# Passkey 认证 (WebAuthn)
webauthn>=2.0.0

# 密码加密
cryptography>=41.0.0

# 系统 Keyring 存储（Mac/Windows 自动可用，Linux 服务器会自动回退到加密文件）
keyring>=24.0.0
